import React, {useState} from 'react';


const InvalidPhoneNotification = () => {;

    return <p>Invalid Phone Number - can only contain numbers with a valid length according to country code</p>
}

export default InvalidPhoneNotification;