const InvalidPasswordNotification = () => {;

    return <p><strong>Invalid Password</strong> - Cannot be empty, must have at least 8 characters, and contain at least one lowercase, uppercase, and number</p>
}

export default InvalidPasswordNotification;